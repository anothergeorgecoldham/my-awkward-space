# Netlify Build Configuration
[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22.23.2"

# Redirect all requests to index.html for SPA routing (not needed for Astro SSG, but good practice)
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
